spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cashlog
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}

logging:
  level:
    com.cashlog: DEBUG
