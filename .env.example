# The backend uses an embedded H2 database (file mode) — no DB credentials or
# separate database container are required.
#
# Optional overrides:
# CASHLOG_DB_PATH=/data/cashlog          # H2 database file prefix
# APP_CORS_ALLOWED_ORIGINS=http://localhost:5173
# SPRING_PROFILES_ACTIVE=demo            # throwaway in-memory database
